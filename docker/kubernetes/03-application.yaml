apiVersion: v1
kind: ServiceAccount
metadata:
  name: exchange-rate-service
  namespace: bank
  labels:
    app.kubernetes.io/name: exchange-rate-service
    app.kubernetes.io/part-of: bank
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: exchange-rate-service
  namespace: bank
  labels:
    app.kubernetes.io/name: exchange-rate-service
    app.kubernetes.io/part-of: bank
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: exchange-rate-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: exchange-rate-service
        app.kubernetes.io/part-of: bank
    spec:
      serviceAccountName: exchange-rate-service
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      containers:
        - name: exchange-rate-service
          # Docker Desktop can use the locally built image with this name.
          image: bank-exchange-rate-service:local
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: exchange-rate-service-config
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: exchange-rate-redis-secret
                  key: REDIS_PASSWORD
          ports:
            - name: grpc
              containerPort: 50053
              protocol: TCP
          startupProbe:
            exec:
              command:
                - /app/bank-exchange-rate-service
                - healthcheck
            periodSeconds: 3
            timeoutSeconds: 3
            failureThreshold: 20
          readinessProbe:
            exec:
              command:
                - /app/bank-exchange-rate-service
                - healthcheck
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            exec:
              command:
                - /app/bank-exchange-rate-service
                - healthcheck
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 16Mi
---
apiVersion: v1
kind: Service
metadata:
  name: exchange-rate-service
  namespace: bank
  labels:
    app.kubernetes.io/name: exchange-rate-service
    app.kubernetes.io/part-of: bank
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: exchange-rate-service
  ports:
    - name: grpc
      port: 50053
      targetPort: grpc
      protocol: TCP
